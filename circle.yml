machine:
  timezone: Asia/Tokyo

dependencies:
  pre:
    - sudo pip install awscli

test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - ./test.php
