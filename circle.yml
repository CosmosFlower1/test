machine:
  timezone: Asia/Tokyo
  php:
      version: 5.5.11

dependencies:
  pre:
    - sudo pip install awscli

test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
      - ./test.php
